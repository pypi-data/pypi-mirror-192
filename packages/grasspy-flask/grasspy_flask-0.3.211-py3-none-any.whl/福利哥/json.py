从 flask.json 导入 jsonify, dumps, dump, loads, load


套路 序列化为json(*参数々, **关键词参数々):
    返回 jsonify(*参数々, **关键词参数々)

套路 转储串(对象, 应用=空, **关键词参数々):
    返回 dumps(对象, app=应用, **关键词参数々)

套路 转储(对象, 文件对象, 应用=空, **关键词参数々):
    返回 dump(对象, 文件对象, app=应用, **关键词参数々)

套路 加载串(json串, 应用=空, **关键词参数々):
    返回 loads(json串, app=应用, **关键词参数々)

套路 加载(文件对象, 应用=空, **关键词参数々):
    返回 load(文件对象, app=应用, **关键词参数々)