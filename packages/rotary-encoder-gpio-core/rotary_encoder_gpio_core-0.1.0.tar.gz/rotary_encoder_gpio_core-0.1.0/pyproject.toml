[tool.poetry]
name = "rotary-encoder-gpio-core"
version = "0.1.0"
description = ""
authors = ["None"]
license = "MIT"
readme = "README.md"
packages = [{include = "rotary_encoder_gpio_core"}]
build = "build.py"

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["setuptools", "poetry-core"]
build-backend = "poetry.core.masonry.api"
