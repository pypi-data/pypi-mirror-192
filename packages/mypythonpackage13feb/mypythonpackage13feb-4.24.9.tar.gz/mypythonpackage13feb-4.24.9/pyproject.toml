[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]
[project]
name = "mypythonpackage13feb"
description = "string"
version = "4.24.9"
requires-python = ">=3.7"
keywords = []
license = {text = "fileString"}
authors = [{name = "string", email = "a@a.com"}]
urls = {}
dependencies = ["apimatic-core~=0.1.0", "apimatic-core-interfaces~=0.1.0", "apimatic-requests-client-adapter~=0.1.0", "python-dateutil~=2.8.1", "enum34~=1.1, >=1.1.10"]
classifiers = []
[project.optional-dependencies]
testutils = ["pytest>=7.1.3"]
