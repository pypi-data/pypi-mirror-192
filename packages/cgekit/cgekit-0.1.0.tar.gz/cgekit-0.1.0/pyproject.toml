[tool.poetry]
name = "cgekit"
version = "0.1.0"
description = "Additional tools for the cgePy library"
authors = ["catbox305 <lion712yt@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
