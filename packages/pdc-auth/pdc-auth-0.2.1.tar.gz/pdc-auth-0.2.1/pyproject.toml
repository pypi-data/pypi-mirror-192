[tool.poetry]
name = "pdc-auth"
version = "0.2.1"
description = "pdc authentication manager"
authors = ["hfrada <madefrada@gmail.com>"]
readme = "README.md"
packages = [{include = "pdc_auth"}]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.28.1"
pydantic = "^1.10.2"
PyYAML = "^6.0"

[tool.poetry.dev-dependencies]
pytest = "^7.1.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
